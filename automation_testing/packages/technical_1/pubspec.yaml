name: technical_1
version: 0.0.1
description: A new Flutter package project.

environment:
  sdk: '>=2.15.1 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: 6.0.0
  audioplayers:
    git:
      url: git@github.com:manabie-com/audioplayers.git
      path: packages/audioplayers

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
